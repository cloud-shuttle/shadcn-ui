{
  "name": "leptos-shadcn-ui",
  "version": "0.1.0",
  "description": "Beautiful, accessible UI components built specifically for Leptos applications",
  "keywords": ["leptos", "ui", "components", "rust", "web", "tailwind", "shadcn"],
  "author": "CloudShuttle",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/cloud-shuttle/leptos-shadcn-ui.git"
  },
  "scripts": {
    "test": "playwright test",
    "build": "cargo build",
    "check": "cargo check",
    "clean": "cargo clean",
    "install:pnpm": "pnpm install",
    "test:playwright": "playwright test",
    "test:playwright:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
